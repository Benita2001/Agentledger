# Agentledger
MCP server that stores AI agent reasoning traces permanently on 0G Storage  immutable, verifiable, tamper-proof. Every decision gets a root hash receipt. Decisions are chained so history cannot be rewritten.
